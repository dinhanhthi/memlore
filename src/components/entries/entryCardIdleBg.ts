/** CSS color the idle `EntryCoverBackdrop` wash must match.
 *
 * Signature lists sit on `--color-selected-tab`. Clean light sits on
 * `--color-panel-3` / elevated. Clean dark sits on `--color-panel-2`. */
export const ENTRY_CARD_IDLE_BG = 'var(--entry-card-idle-bg)' as const
